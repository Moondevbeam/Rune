import { GlassCard, type GlassCardProps } from '@/components/wallet/GlassCard';

export type SurfaceCardProps = GlassCardProps;

/** @deprecated Prefer GlassCard directly — SurfaceCard is a thin alias for backward compat. */
export const SurfaceCard = GlassCard;
